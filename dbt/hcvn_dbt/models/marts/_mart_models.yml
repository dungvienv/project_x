version: 2

models:
  - name: mart__crm__rep_bod0_2_application
    columns:
      - name: dbt_key_skp_bod0_2_application
        # tests:
        #   - unique
        #   - not_null
    config:
      enabled: true
      materialized: incremental
      incremental_strategy: merge
      unique_key: dbt_key_skp_bod0_2_application
      incremental_predicates : [
        "DBT_INTERNAL_SOURCE.date_bod0 >= sysdate - 5",
        "DBT_INTERNAL_SOURCE.date_bod01 >= sysdate - 5",
      ]
      tags: ['crm']

  - name: mart__mkt__bod0_2_source
    columns:
      - name: dbt_key_skp_bod0_2_application
        # tests:
        #   - unique
        #   - not_null
    config:
      enabled: true
      materialized: incremental
      incremental_strategy: merge
      unique_key: dbt_key_skp_bod0_2_application
      incremental_predicates : [
        "DBT_INTERNAL_SOURCE.lead_timestamp >= sysdate - 30" 
      ]
      tags: ['crm']
